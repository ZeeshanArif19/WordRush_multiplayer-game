apiVersion: v1
kind: ConfigMap
metadata:
  name: wordrush-config
spec:
  # Note: ConfigMaps are for non-sensitive data
  PORT: "3001"
  ALLOWED_ORIGINS: "https://wordrushgame.vercel.app,http://localhost:5173"
