apiVersion: v1
kind: Service
metadata:
  name: wordrush-backend-service
spec:
  selector:
    app: wordrush-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
  type: LoadBalancer
