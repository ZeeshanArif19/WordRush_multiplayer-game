apiVersion: v1
kind: Service
metadata:
  name: wordrush-backend-service
spec:
  selector:
    app: wordrush-backend
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
      nodePort: 30001
  type: NodePort
