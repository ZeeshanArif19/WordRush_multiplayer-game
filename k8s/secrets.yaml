apiVersion: v1
kind: Secret
metadata:
  name: wordrush-secrets
  namespace: wordrush
type: Opaque
data:
  # Values must be base64 encoded. 
  # Use: echo -n "your-db-url" | base64
  database-url: REPLACE_WITH_BASE64_DB_URL
